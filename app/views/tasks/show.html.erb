<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed ? "Completed" : "Incomplete" %></p>
<p><%= link_to "Edit task", edit_task_path(@task) %> | <%= link_to "Back to tasks", tasks_path %></p>
